server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: spring-boot-file-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: spring-boot-logs
          __path__: /workspace-logs/*.log

    pipeline_stages:
      - regex:
          expression: '^(?P<ts>\S+)\s+(?P<level>\S+)\s+\[(?P<service>[^,\]]+),trace=(?P<traceId>[^,]*),span=(?P<spanId>[^\]]*)\]'
      - labels:
          level:
          service:
          traceId:
          spanId:

